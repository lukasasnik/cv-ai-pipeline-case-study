[project]
name = "cv-ai-pipeline-case-study"
version = "0.1.0"
description = "CV AI Processing Pipeline Case Study"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = [
    "api",
    "database",
    "file-server",
    "shared",
]
